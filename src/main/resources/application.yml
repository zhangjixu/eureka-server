server:
  port: 8761
spring:
  application:
    name: eureka-erver
eureka:
  client:
    register-with-eureka: false # 是否向服务注册中心注册自己
    fetch-registry: false # 是否检索服务
  server:
    enable-self-preservation: false # 是否开启保护机制