server:
  port: 8761
spring:
  application:
    name: eureka-erver
eureka:
  client:
    # 是否向服务注册中心注册自己
    register-with-eureka: false
    # 是否检索服务
    fetch-registry: false
  server:
    # 关闭保护机制
    enable-self-preservation: false